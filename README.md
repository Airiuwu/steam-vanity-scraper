# steam-vanity-scraper
searches for x amount character vanity URL

# Usage
First, install the requirements `pip install -r requirements.txt` then Set your API Key in `config.py` then
execute `main.py`
